export enum AudienceType {
    Adult = 'adult',
    Child = 'child',
    Student = 'student',
    Senior = 'senior',
    Tourist = 'tourist'
}

export enum ExcursionFormat {
    Regular = 'regular',
    Custom = 'custom'
}

export enum BookingStatus {
    Confirmed = 'confirmed',
    Requested = 'requested'
}
